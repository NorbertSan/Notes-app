import React from 'react';
import { storiesOf } from '@storybook/react';
import Heading from './Heading';

storiesOf('Atoms/Heading', module).add('Default', () => (
  <Heading>New features in css !</Heading>
));
